{
	"associations/_association.html": {
		{% for name in (config.cur|query("select `Name` from `datasets`")).data|one_tuple %}
			{% for typ in config.typs %}
				"{{ "associations/%s_%s.html"|format(urlize(name), typ) }}": {
					"name": "{{ name }}",
					"typ": "{{ typ }}",
					"uri": "{{ urlize(name) }}",
				},
			{% endfor %}
		{% endfor %}
	},
	"associations/_association_clustergram.json": {
		{% for name in (config.cur|query("select `Name` from `datasets`")).data|one_tuple %}
			{% for typ in config.typs %}
				"{{ "associations/%s_%s_clustergram.json"|format(urlize(name), typ) }}": {
					"name": "{{ name }}",
					"typ": "{{ typ }}"
				},
			{% endfor %}
		{% endfor %}
	}
}
